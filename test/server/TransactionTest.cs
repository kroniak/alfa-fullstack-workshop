using Server.Exceptions;
using Server.Models;
using Xunit;

namespace ServerTest
{
    public class TransactionTest
    {
        
        /*[Theory]
        [InlineData(null, 10, "description")]
        [InlineData("5395029009021990", 0, "description")]
        [InlineData("5395029009021990", 10, null)]
        public void CheckTransactionException(string cardNumber, decimal amount, string description)
        {
            Assert.Throws<TransactionDataException>(() => new Transaction(cardNumber, amount, description));
        }*/
    }
}