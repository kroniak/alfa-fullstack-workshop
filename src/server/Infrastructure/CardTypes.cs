using System;

namespace Server.Infrastructure
{
    /// <summary>
    /// Enum for card types
    /// </summary>
    public enum CardType {
        MASTERCARD = 1,
        VISA,
        MAESTRO,
        MIR
    }
}